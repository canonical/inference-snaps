environment:
# Default OpenAI base path for llama.cpp HTTP server
- OPENAI_BASE_PATH=/v1
# Path to the llama-server binary within the component (built during packaging)
- SERVER_PATH=$COMPONENT/bin/llama-server
# Append path to shared objects 
- LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$COMPONENT/lib
